{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Server",
            "program": "${workspaceFolder}/server.js",
            "outFiles": ["${workspaceFolder}/src/**/*.js"],
            "restart": true,
            "console": "integratedTerminal"
        }
    ]
}